// Número máximo de cabeceras
var maxCabeceras = 6;

// Escribir las cabeceras h1 a h6 utilizando document.write
for (var i = 1; i <= maxCabeceras; i++) {
    document.write("<h" + i + ">Cabecera h" + i + "</h" + i + ">");
}


