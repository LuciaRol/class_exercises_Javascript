/**
 function stringLength(str){
    let length = str.length
    console.log(`the length of "${str}" is:`, length)
    return str.length
}

stringLength("willynilly")
 */

var stringLength = "willynilly";
str => length = str.length;
    console.log(`La longitud de "${str}" es:`, length);
    return str.length;






